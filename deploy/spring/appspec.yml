version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/keep4life/spring
permissions:
  - object: /
    owner: ubuntu
    group: ubuntu
hooks:
  BeforeInstall:
    - location: scripts/before-spring.sh
      timeout: 60

  ApplicationStart:
    - location: scripts/start-spring.sh
      timeout: 300
      runas: ubuntu
